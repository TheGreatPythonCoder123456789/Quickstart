package org.firstinspires.ftc.teamcode.HP_Autonomous;

public class Start_TheAutonomouses_ForThis {

}

/*a
public static class Paths {

  public PathChain Path1;

  public Paths(Follower follower) {
    Path1 = follower
      .pathBuilder()
      .addPath(
        new BezierLine(new Pose(88.000, 9.000), new Pose(81.000, 22.000)) // Start Pose, New Pose
      )
      .setLinearHeadingInterpolation(Math.toRadians(270), Math.toRadians(-114)) //start heading, new heading
      .build();
  }
}


{ Pose on driver hub: X: -1.1055 Y: 4.1048 Heading (deg): -119.2407 } {Robot started to the right side from back
like on visualizer 0 degrees}

private void shootAllBalls() throws InterruptedException {

        servoSetter();
        waitForShooterReady();

        // Ball 1
        intakeTop.setPower(-1.0);
        sleep(250);
        intakeTop.setPower(0.8);
        sleep(150);
        intakeTop.setPower(0);
        sleep(733);

        // Ball 2
        intakeTop.setPower(-1.0);
        sleep(250);
        intakeTop.setPower(0.8);
        sleep(150);
        intakeTop.setPower(0);
        sleep(533);

        // Ball 3
        intakeTop.setPower(-1.0);
        sleep(800);
        intakeTop.setPower(0);

        shooter.stopShooter();
        gate.setPosition(1.0);
    }
*/
